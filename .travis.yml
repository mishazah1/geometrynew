compiler : gcc

                   dist : xenial

                                  language : C script : -git ls
                                                        - files src /
        | xargs clang - format - i&& git diff —exit - code&& make
